const {Server} = require('./server'); // se esta exportando de server.js

const server = new Server(); // se ejecuta lo que esta en estructor

server .start(); //se inicia el servicio start de la exportacion 